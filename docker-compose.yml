version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: macxgain-app
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "9000:9000"
    volumes:
      - ./storage:/var/www/html/storage
      - ./public/files:/var/www/html/public/files
      - ./logs:/var/log
      - cache_data:/var/cache/laravel
      - redis_cache:/var/cache/redis
      - websocket_logs:/var/log/websocket
      - truedata_logs:/var/log/truedata
      - scheduler_logs:/var/log/scheduler
      # User uploads and trading media (host volumes for persistence)
      - /var/macxgain-data/trading:/var/www/html/public/trading
      - /var/macxgain-data/uploads:/var/www/html/public/uploads
      - /var/macxgain-data/profiles:/var/www/html/public/profiles
      - /var/macxgain-data/screenshots:/var/www/html/public/screenshots
      - /var/macxgain-data/documents:/var/www/html/public/documents
    environment:
      - APP_ENV=production
      - APP_DEBUG=false
      - APP_URL=${APP_URL:-https://macxgain.com}
      - DB_CONNECTION=mysql
      - DB_HOST=host.docker.internal
      - DB_PORT=3306
      - DB_DATABASE=macxgain
      - DB_USERNAME=macxgain
      - DB_PASSWORD=macxgain123
      - REDIS_HOST=redis
      - REDIS_PASSWORD=null
      - REDIS_PORT=6379
    depends_on:
      - redis
    networks:
      - macxgain-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  redis:
    image: redis:7-alpine
    container_name: macxgain-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - macxgain-network

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: macxgain-phpmyadmin
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      PMA_HOST: host.docker.internal
      PMA_USER: macxgain
      PMA_PASSWORD: macxgain123
    networks:
      - macxgain-network

volumes:
  redis_data:
    driver: local
  cache_data:
    driver: local
  redis_cache:
    driver: local
  websocket_logs:
    driver: local
  truedata_logs:
    driver: local
  scheduler_logs:
    driver: local
  # Host volumes used for user data - no need to define here

networks:
  macxgain-network:
    driver: bridge
